# Test Fixtures

This folder hosts shared fixtures used by core, server, and CLI tests.

- `graphs/`: Sample graph JSON payloads.
- `datasets/`: CSV/XLSX/ODS fixtures for import/export tests.
- `plans/`: YAML plan fixtures.
- `golden/errors/`: GraphQL error baselines (JSON responses).
- `golden/exports/`: Export output baselines.
