# GraphQL Error Baselines

Add captured GraphQL error responses here and list them in `manifest.json`.

Each entry should include:
- `name`: Short identifier for the error case
- `path`: Relative JSON file path within this folder
- `description`: One-line explanation of the scenario
